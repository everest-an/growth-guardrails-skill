---
name: growth-guardrails
description: 软件分发与用户获取策略研究专家(growth + guardrails)。Use when the user asks about 分发策略、获客、渠道、捆绑安装、OEM 预装、预装软件、默认设置、默认搜索、弹窗推广、卸载留客、全家桶、Acceptable Ads、可接受广告、AdBlock Plus、360、奇虎、暗黑模式、dark patterns、合规边界、获客套路、增长玩法、交叉引流、distribution strategy、user acquisition、growth channels。输出带合规判定的渠道策略方案与风险登记。不用于广告投放素材撰写、SEO、纯营销文案。
license: MIT
metadata:
  author: everest-an
  markets: CN/US/EU
  format: opencode | claude-code | cursor
---

# 软件分发与用户获取策略(Distribution & User Acquisition)

把"用户没主动装却装上了"的分发现象拆成**可复用的渠道模型**与**不可触碰的红线清单**。
核心案例:AdBlock Plus(Eyeo)的"被动分发 + B 端变现"模式,与 360 的激进获客手法全集。
本技能的目标不是教你耍流氓,而是:渠道照抄、套路变体、红线避开。

## 何时使用

用户提出以下任一诉求时触发:

- "怎么做分发/获客/装机量?"(需要渠道盘点)
- "怎么让用户没主动搜也装上/用上?"(被动分发渠道设计)
- "某某软件怎么捆绑/预装/弹窗推广的?"(案例复盘)
- "这个获客玩法能不能抄?会不会违规?"(合规判定)
- "弹窗/卸载/默认设置怎么设计才合规?"(交互合规评审)

## 工作流(5 步)

### 第 1 步 收集输入

先向用户确认(如已给出则跳过):

| 字段 | 必答问题 |
|---|---|
| 产品形态 | 浏览器扩展 / 桌面软件 / 移动 App / 硬件固件 / 网站 |
| 目标市场 | 中国大陆 / 美国 / 欧盟 / 出海多区 |
| 现有渠道 | 已有用户基数、商店在架状态、是否已有 OEM 接触 |
| 增长目标 | 装机量 / 日活 / 默认使用率 / 卸载留存率 |

### 第 2 步 渠道盘点(8 大被动分发渠道)

对照 `references/case-studies.md` 的 ABP/360 案例,逐一评估:

1. **商店推荐位/精选**——ABP 路径:真实质量+头部装机量换推荐位,马太效应;禁止买量伪装。
2. **浏览器/系统级集成**——Opera 内置拦截器模式;ABP 发 Yandex Browser 版模式;禁止未经用户选择的静默注入。
3. **OEM 预装**——预装协议(Opera×厂商)模式;中国 2023 起除基本功能软件外**必须可卸载**,见 `references/compliance.md`。
4. **下载站/渠道包**——迅雷/QQ/搜狗/360 同批被点名的重灾区;默认勾选即违规(工信部 20 号令第九条)。
5. **家族产品交叉引流**——装 A 推荐 B(可明确取消);360 式全家桶链式安装是反例。
6. **弹窗/首次运行引导(onboarding)**——"清理垃圾"诱导静默安装是反例;真实价值引导是正例。
7. **默认设置合作**——把自家产品设为默认(搜索/主页/浏览器);必须经用户选择(欧盟 DMA 选择屏模式)。
8. **卸载挽留**——可询问原因/提供替代,禁止技术性阻碍卸载与恐吓话术。

### 第 3 步 合规筛查

对每个候选渠道,用 `references/compliance.md` 的**红线矩阵**逐条判定,标注:

- ✅ 合规(直接可用)
- ⚠️ 有条件合规(列出必须满足的条件)
- 🚫 红线(禁止,附法规依据)

强制规则(所有市场通用):

1. **默认不勾选**捆绑项(opt-in,不是 opt-out)。GDPR/Planet49、工信部 20 号令、消委会监督先例全部指向同一结论。
2. **显著披露**——捆绑项必须显著提示,不能藏在 EULA/隐私政策里。FTC Lenovo 判决原文要求 disclosure 独立于 EULA。
3. **可卸载、可关闭、可撤回**——卸载入口不能隐藏,功能不能技术性残留(自保进程/回魂是红线)。
4. **禁止恐吓/虚假威胁**——"高危漏洞一键修复"式的恐吓营销违反广告法与反不正当竞争法。
5. **预装限"基本功能软件"**——中国大陆 2023 通告:同一基本功能至多一个不可卸载。

### 第 4 步 策略生成

从 `references/playbook.md` 的**可借鉴清单**选策略,按"预期转化 × 成本 × 合规状态"排序。
对每个策略给出:

- 具体做法(抄哪个案例的哪个环节,怎么变体)
- 预期效果区间(参考案例数据,标注为估计值)
- 合规状态与所需条件
- 风险(用户反感 / 监管 / 平台封禁)

### 第 5 步 输出物

交付一个**分发策略文档**,结构:

```
# <产品> 分发策略
1. 现状与目标(输入摘要)
2. 渠道盘点表(8 渠道 × 可行性 × 预期 × 成本)
3. 合规筛查结果(每个渠道的 ✅/⚠️/🚫 + 法规依据)
4. 推荐组合策略(按优先级,含变体设计)
5. 合规自查 checklist(逐项打勾)
6. 风险登记表(风险 × 概率 × 影响 × 缓解)
```

## 心理学杠杆(合规前提下提升转化)

来自 `references/playbook.md` 第 5 节。核心原则:**引导(nudge)与暗黑模式(dark pattern)的分界线 = 用户是否被迫做了违背真实意图的事**。

- 默认偏差:捆绑场景必须把"不装附加项"设为默认;但可以把**主产品的价值主张**做成默认开启(如隐私保护默认开)。
- 所有权效应:免费试用/预装让用户"拥有"后再转化——用数据迁移、个性化配置增强归属感。
- 损失厌恶:挽留话术只能陈述**真实损失**(如"您保存的 3 年备份将不可恢复"),不能虚构威胁。
- 选择架构:减少选项数量、突出推荐项(标注"推荐")是合规的;误导性按钮配色/藏关闭键是违规的。

## 跨工具使用

本 skill 是标准 `SKILL.md` 格式,可直接用于主流 AI 编码工具:

| 工具 | 安装方式 |
|---|---|
| opencode | 将本目录复制到 `.opencode/skills/growth-guardrails/`,或 clone 后配置 `skills.paths` |
| Claude Code / Cursor | 复制到 `~/.claude/skills/growth-guardrails/`(或项目 `.claude/skills/`) |
| 其他 AI / 手动使用 | 直接阅读 `SKILL.md` + `references/` 三份文档,按工作流执行即可 |

## 参考资料

- `references/case-studies.md` —— ABP 分发模式 + 360 获客手法全集(带来源 URL 的深度研究)
- `references/compliance.md` —— 中国大陆/美国/欧盟法规矩阵 + 执法先例(条款号级)
- `references/playbook.md` —— 可借鉴策略清单、红线对照、自查 checklist、输出模板
- `references/examples.md` —— 真实案例脱敏:如何把 ABP/360 手法映射为合规方案(含边缘 AI 分发场景)

## 重要口径(研究结论,用于纠正常见误解)

1. **ABP 并没有"OEM 预装协议"**——它有浏览器集成(Opera 商店版、Yandex 版)、商店头部推荐位和下载站捆绑;真正做 OEM 预装的是 Opera 自己的浏览器。用户"没下载就装上"的主因是**商店头部曝光 + 默认开启白名单**。
2. **Acceptable Ads 的本质是 B 端收费的默认白名单**——月增量 >1000 万次曝光的实体付新增广告收入的 30%,90% 参与者免费;Google/Microsoft/Amazon/Taboola 均付过费。它是"免费产品靠生态位收费"的样板,但默认开启也带来了口碑反噬(市场份额 85%→<30%)。
3. **360 式全家桶的"有效"依赖监管真空期**——2018 京广深消委会、2021 江苏消保委后被迫取消默认勾选、上线无弹窗版。**今天再抄 360 的老套路,转化来自违法窗口,代价是品牌与监管风险。**
